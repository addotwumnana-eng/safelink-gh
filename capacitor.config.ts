import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.addo.safelinkghana',  // Change this to your unique package ID
  appName: 'SafeLink Ghana',         // Change this to the display name
  webDir: 'dist'
};

export default config;
